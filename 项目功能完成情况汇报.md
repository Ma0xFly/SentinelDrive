# SentinelDrive 项目功能完成情况汇报

**汇报日期：** 2026-06-15  
**项目仓库：** `https://github.com/Ma0xFly/SentinelDrive`（私有仓库）  
**部署目标：** Linux 服务器 / Ubuntu 22.04 / Docker Compose

## 一、总体结论

SentinelDrive 已完成车联网威胁情报平台 MVP 的主体建设，当前代码仓库已经具备在单台 Linux 服务器上通过 Docker Compose 部署运行的能力。项目已覆盖后端 API、中文前端工作台、威胁情报采集 Worker、定时调度、PostgreSQL 持久化、Redis 队列/缓存、Caddy 反向代理、初始化管理员、数据迁移、运维命令和中文化项目文档。

从交付状态看，项目不是单一页面或脚本原型，而是一个完整的 Web 平台运行栈。APM 任务追踪中 Stage 1 到 Stage 7 均已完成，功能范围覆盖“采集 -> 原始入库 -> 规范化 -> 去重 -> 风险评分 -> 告警 -> 检索/详情/导出 -> 前端运营工作台”的 MVP 主链路。

当前阶段适合进行服务器部署、真实数据源配置、上线前联调和小范围试运行。

## 二、项目定位

SentinelDrive 面向车联网安全研究和安全运营场景，用于持续收集公开漏洞、厂商公告、安全博客、CISA KEV、NVD CVE 以及人工录入的威胁情报，并将这些来源不同、格式不同的信息规范化为车联网领域可分析的数据。

项目重点解决以下问题：

- **情报分散：** NVD、CISA KEV、RSS、厂商公告和人工线索分散在不同入口，人工跟踪成本高。
- **车联网语义不足：** 通用漏洞库缺少 T-Box、OTA、IVI、V2X、Charging、Cloud API、CAN 等车辆领域分类。
- **重复情报难处理：** 同一 CVE 或同一公告可能被多个来源重复报道，需要去重和来源归并。
- **风险优先级不清晰：** 仅看 CVSS 不足以判断车联网真实风险，需要结合 KEV、远程可利用性、车辆关键组件、来源可信度等因素评分。
- **缺少运营工作台：** 需要中文界面支持搜索、筛选、详情查看、告警处理、数据源状态跟踪和人工录入。

## 三、已完成的核心功能

### 1. Linux 服务器部署运行栈

项目已完成面向 Linux 服务器的容器化部署结构，包含以下服务：

| 服务 | 作用 | 完成情况 |
| --- | --- | --- |
| `reverse-proxy` | Caddy 反向代理，提供 HTTP/HTTPS 入口，并代理 `/api/*` | 已完成 |
| `frontend` | Next.js 中文前端工作台 | 已完成 |
| `backend` | FastAPI 后端 API 服务 | 已完成 |
| `worker` | Celery Worker，执行采集、规范化、评分等任务 | 已完成 |
| `scheduler` | Celery Beat 定时调度器 | 已完成 |
| `postgres` | PostgreSQL 持久化数据库 | 已完成 |
| `redis` | Redis 队列、缓存和 Celery broker/result backend | 已完成 |

部署侧已提供：

- `docker-compose.yml`：完整服务编排。
- `.env.example`：生产部署环境变量模板。
- `Makefile`：统一封装配置检查、启动、迁移、管理员初始化、日志、同步和处理命令。
- `deploy/caddy/Caddyfile`：反向代理配置。
- `.gitattributes`：统一换行规则，降低 Windows 开发、Linux 部署之间的脚本兼容风险。

常用部署命令已经固化：

```bash
make config-check
make compose-config
docker compose up -d --build
make migrate
make admin-bootstrap
```

### 2. 后端 API 与领域模型

后端采用 FastAPI，已经完成核心 API 和领域模型建设。

已完成能力包括：

- 健康检查与就绪检查：`/api/health`、`/api/ready`。
- 配置加载与环境变量校验。
- PostgreSQL 和 Redis 连通性探测。
- 用户认证、Token、密码哈希。
- 管理员初始化脚本。
- 基础用户 API。
- 审计日志能力。
- 数据源管理 API。
- 情报搜索与详情 API。
- 人工录入 API。
- 告警列表、详情、状态更新 API。
- 导出 API。
- Alembic 数据库迁移。

后端模型覆盖：

- 用户与安全凭证。
- 数据源配置。
- 同步状态。
- 任务日志。
- 原始情报。
- 规范化威胁情报。
- 告警。
- 导出记录。
- 审计事件。

这些模型为后续扩展搜索引擎、对象存储、多 Worker 节点和更细的权限控制预留了边界。

### 3. 威胁情报采集与处理管道

Worker 侧已经完成 Source Connector 架构和主处理链路。

已完成数据源类型：

| 数据源类型 | 功能说明 | 完成情况 |
| --- | --- | --- |
| NVD CVE | 支持 NVD API 2.0，可配置 API key，默认不强制全量历史导入 | 已完成 |
| CISA KEV | 支持已知被利用漏洞目录采集 | 已完成 |
| RSS | 支持安全博客、研究团队、厂商安全中心等 Feed 配置 | 已完成 |
| 厂商公告 | 支持厂商公告入口和代表性公告源配置 | 已完成 |
| 人工录入 | 支持通过后端 API 和前端页面录入漏洞、公告、事件、暴露面 | 已完成 |

处理管道已经覆盖：

```text
Source Connector -> Raw Intelligence -> Normalize -> Deduplicate -> Threat Intelligence -> Search / Score / Alert
```

具体能力包括：

- Connector 合约与注册表。
- HTTP 拉取、超时、重试、限速配置。
- 原始采集结果持久化。
- 规范化模型与 normalizer。
- 基于 CVE ID、来源 URL、标题、内容哈希等维度的去重。
- 多来源归并和 `last_seen_at` 更新。
- 固定规则风险评分。
- 处理一次完整管道的命令：`make process-once`。
- 单独触发同步的命令：`make sync-once`。

### 4. 风险评分与告警能力

项目已实现透明、可测试的固定规则风险评分，而不是依赖不可解释的 AI 判断。

评分因素包括：

- CVSS 分数。
- 是否进入 CISA KEV。
- 是否存在公开利用信号。
- 是否远程可利用。
- 是否需要认证。
- 是否影响车辆关键组件。
- 是否涉及 OTA、T-Box、Cloud API、CAN、V2X、Charging 等关键攻击面。
- 是否影响多个厂商或通用组件。
- 来源可信度。

告警能力覆盖：

- 新增 Critical 情报。
- 新增 CISA KEV 漏洞。
- 影响关键车辆组件或关键攻击面的高风险情报。
- 同一 CVE 被多个来源重复观察。
- 同一厂商或组件短时间内出现多条高风险情报。
- 告警状态更新与审计。

### 5. 中文前端安全运营工作台

前端采用 Next.js / React，已实现中文安全运营工作台，不是营销页或静态展示页。

已完成页面包括：

| 页面 | 功能 |
| --- | --- |
| 情报列表 | 搜索、筛选、排序、风险与状态展示 |
| 情报详情 | 来源链接、摘要、影响对象、车联网分类、评分解释、关联告警 |
| 告警列表 | 风险等级、触发规则、状态展示和状态更新 |
| 数据源状态 | 启用状态、最近同步时间、失败次数、最近错误 |
| 人工录入 | 录入漏洞、公告、事件、暴露面和研究线索 |
| 用户基础功能 | 登录态、基础用户工作台能力 |

前端已经接入共享 API client 和数据 hook，并提供 Playwright E2E 用例覆盖运营流程。

### 6. 导出能力

项目已完成 MVP 范围内的导出能力：

- 情报列表导出为 CSV。
- 告警列表导出为 CSV。
- 单条情报详情导出为 Markdown。
- 单条情报或告警摘要生成基础 PDF 报告。

导出接口走认证保护，避免直接暴露敏感配置、Token 或不应在前端展示的原始数据。

### 7. 运维、备份与文档

项目已经补齐中文化运维和开发文档，便于后续部署、维护和交接。

已完成文档包括：

- `README.md`：Linux 服务器部署入口文档。
- `docs/deployment.md`：Ubuntu Docker Compose 部署指南。
- `docs/operations.md`：运维手册。
- `docs/environment.md`：环境变量说明。
- `docs/development.md`：本地开发指南。
- `docs/connectors.md`：Connector 开发指南。
- `docs/source-configuration.md`：数据源配置示例。
- `docs/testing.md`：测试与 QA 指南。
- `docs/qa/compose-deployment-verification.md`：Compose 部署验证。
- `.apm/`：AI 项目管理过程文档、计划、追踪和交接记录。
- `.agents/`、`.codex/`：已替换为中文化 APM 运行时技能和指南。

运维命令已覆盖：

```bash
make logs
make migrate
make admin-bootstrap
make sync-once
make process-once
make down
```

备份方案已覆盖 PostgreSQL 数据库备份，并要求在迁移、升级、批量导入或恢复演练前先执行备份。

## 四、质量保障情况

项目包含后端、Worker 和前端多个层面的测试文件。

后端测试覆盖：

- 健康检查。
- 配置加载。
- 数据库迁移。
- ORM 模型。
- 管理员初始化。
- 认证。
- 数据源 API。
- 情报 API。
- 告警 API。
- 人工录入。
- 导出 API。
- 告警评估脚本。

Worker 测试覆盖：

- Connector 合约。
- NVD / CISA Connector。
- RSS / 厂商公告 Connector。
- 原始采集持久化。
- PostgreSQL 类型处理。
- 规范化管道。
- 处理管道。
- 风险评分服务。

前端测试覆盖：

- Next.js 检查命令。
- Playwright 运营工作流 E2E 用例。

当前仓库已经具备较完整的测试结构，后续上线前建议在目标 Linux 服务器或等价环境中执行一次完整部署验证，包括 `docker compose up -d --build`、数据库迁移、管理员初始化、数据同步、前端页面访问和告警处理流程。

## 五、当前交付物清单

| 类型 | 交付物 |
| --- | --- |
| 代码仓库 | 私有 GitHub 仓库 `Ma0xFly/SentinelDrive` |
| 后端 | FastAPI 应用、API route、认证、安全、模型、迁移、脚本、测试 |
| 前端 | Next.js 中文工作台、页面、组件、API client、E2E 测试 |
| Worker | Celery、Connector、采集、规范化、去重、评分、持久化、测试 |
| 部署 | Docker Compose、Caddy、环境变量模板、Makefile |
| 数据库 | PostgreSQL schema、Alembic 迁移、持久化卷配置 |
| 队列/缓存 | Redis 配置、Celery broker/result backend |
| 文档 | README、部署、运维、环境变量、测试、Connector、数据源配置、QA 文档 |
| AI 工程管理 | 中文化 APM 计划、追踪、交接、skills 和 Codex guides |

## 六、服务器部署方式

服务器具备 GitHub 私有仓库访问权限后，可直接克隆部署：

```bash
cd /opt
git clone git@github.com:Ma0xFly/SentinelDrive.git sentineldrive
cd sentineldrive
cp .env.example .env
nano .env
```

完成 `.env` 生产配置后执行：

```bash
make config-check
make compose-config
docker compose up -d --build
make migrate
make admin-bootstrap
```

部署后检查：

```bash
curl http://127.0.0.1/api/health
curl http://127.0.0.1/api/ready
docker compose ps
```

如果使用域名和 HTTPS，由 Caddy 负责入口代理和证书配置；如果仅临时内网预览，可使用服务器 IP 暴露 HTTP。

## 七、已知边界与上线前建议

当前项目已经完成 MVP 主体功能，但正式上线前建议重点处理以下事项：

1. **生产环境密钥替换：** 必须替换 `.env` 中的 `APP_SECRET_KEY`、`POSTGRES_PASSWORD`、`ADMIN_BOOTSTRAP_PASSWORD` 等值。
2. **GitHub 私有仓库访问：** 服务器需要配置 SSH key 或 Deploy Key，才能克隆私有仓库。
3. **真实数据源验证：** NVD、CISA KEV、RSS、厂商公告需要在服务器网络环境下验证访问频率、限速和解析稳定性。
4. **首次数据量控制：** NVD 默认不应直接全量导入历史数据，避免在 4 GB 内存和有限带宽服务器上造成压力。
5. **备份策略落地：** 上线前应完成一次 PostgreSQL 备份和恢复演练。
6. **公网安全控制：** 生产环境只暴露 SSH、HTTP、HTTPS，不暴露 PostgreSQL、Redis、backend 内部端口。
7. **上线验收测试：** 建议执行一次完整链路：管理员初始化 -> 登录 -> 配置数据源 -> 同步 -> 规范化 -> 评分 -> 告警 -> 导出。

## 八、下一阶段建议

建议下一阶段以“部署试运行 + 真实数据校准”为主，不急于增加大功能。

优先级建议如下：

| 优先级 | 工作项 | 目标 |
| --- | --- | --- |
| P0 | 部署到个人 Linux 服务器 | 验证 Compose、Caddy、迁移、管理员初始化和前端访问 |
| P0 | 配置 GitHub 私有仓库服务器访问 | 保证后续可以通过 `git pull` 更新部署 |
| P0 | 运行完整验收链路 | 确认采集、处理、评分、告警和导出闭环可用 |
| P1 | 接入真实 RSS 与厂商公告源 | 提升车联网情报覆盖面 |
| P1 | 校准风险评分规则 | 根据实际样本调整车辆关键组件、攻击面和来源可信度权重 |
| P1 | 做一次备份恢复演练 | 确认升级和故障恢复能力 |
| P2 | 引入更强搜索能力 | 在数据规模增长后考虑 Meilisearch 或 OpenSearch |
| P2 | 增加通知渠道 | 后续可接入邮件、飞书、企业微信等告警通知 |

## 九、汇报总结

SentinelDrive 当前已经完成从工程脚手架到业务闭环的 MVP 建设，具备部署到 Linux 服务器进行试运行的条件。项目的核心价值在于将分散的车联网安全情报统一采集、规范化、去重、评分和告警，并通过中文工作台提供可操作的安全运营界面。

短期重点应放在服务器部署、真实数据源验证、完整链路验收和备份恢复演练。完成这些工作后，项目即可进入小范围试用阶段，再根据实际情报质量和运营反馈决定是否扩展搜索引擎、通知渠道、多用户权限和更复杂的分析能力。
