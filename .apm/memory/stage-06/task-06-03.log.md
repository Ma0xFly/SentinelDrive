---
stage: 6
task: 3
title: Operator and Developer Documentation
agent: qa-documentation-agent
status: Success
important_findings: true
compatibility_issues: false
---

# Task 6.3 - Operator and Developer Documentation

## Summary
Updated the operator and developer documentation to match the implemented MVP, Compose deployment verification, source collection behavior, and current validation commands. The documentation-only changes were committed on `docs/operator-guide` as `2885a41`.

## Details
Worked in `/home/myx/SentinelDrive/.apm/worktrees/docs-operator-guide`. Reviewed README, development, deployment, environment, Connector, source configuration, testing, operations, scaffold, and QA documentation before editing. Tightened stale scaffold/placeholder language, documented WSL/local preview startup with `CADDY_HOST=:80`, added direct local validation commands, expanded Ubuntu deployment initialization/source/reverse-proxy guidance, documented worker/scheduler egress implications, and clarified that source collection, normalization, scoring, and backend alert generation are separate actions.

## Output
- Updated `README.md`.
- Updated `docs/development.md`.
- Updated `docs/deployment.md`.
- Updated `docs/environment.md`.
- Updated `docs/connectors.md`.
- Updated `docs/testing.md`.
- Updated `docs/operations.md`.
- Updated `docs/scaffold.md`.
- Updated `docs/qa/api-integration-checklist.md`.
- Updated `docs/qa/compose-checklist.md`.
- Updated `docs/qa/frontend-page-checklist.md`.
- Updated `docs/qa/security-secrets-checklist.md`.
- Commit: `2885a41 docs: update operator and developer guides`.

## Validation
Passed:

- `make config-check`
- `make compose-config`
- Markdown link validation for `README.md` and `docs/**/*.md`
- Makefile command reference validation across docs
- `git diff --check`
- `/home/myx/SentinelDrive/.venv/bin/python -m pytest tests -q` from the worktree backend directory: 69 passed, 1 Starlette dependency pending deprecation warning
- `/home/myx/SentinelDrive/.venv/bin/python -m pytest worker/tests -q` from the worktree root: 73 passed
- `npm ci` in the worktree frontend directory: installed ignored local dependencies, 0 vulnerabilities
- `npm run check` in the worktree frontend directory: Next.js production build passed
- `npm audit --omit=dev` in the worktree frontend directory: 0 vulnerabilities

## Issues
The documented backend command `cd backend && ../.venv/bin/python -m pytest tests -q` could not run inside this separate worktree because that worktree does not contain its own `.venv`. I reran the same backend suite with the shared project interpreter at `/home/myx/SentinelDrive/.venv/bin/python`, which passed. The frontend worktree initially lacked `node_modules`; `npm ci` installed ignored dependencies before the successful frontend build and audit.

## Important Findings
The worker tests in this branch now report 73 passing tests, while the prior coverage summary recorded 57. This reflects the current post-Compose-verification test state and was validated during this task. Documentation now distinguishes one-shot collection (`make sync-once`) from normalization, scoring, and backend alert generation so operators do not assume collection performs the full processing pipeline.
