---
stage: 10
task: 3
title: 垂直信源扩展（NHTSA 召回 + vendor 端点）
agent: intelligence-pipeline-agent
status: Success
important_findings: true
compatibility_issues: false
---

# Task 10.3 - 垂直信源扩展（NHTSA 召回 + vendor 端点）

## Summary

已完成 worker 侧 NHTSA 召回 Connector（软件/OTA 召回按 `incident` 入库，机械召回采集层过滤）与厂商公告端点扩充，并同步 Connector/数据源/环境文档。worker 全量 105 测试通过。

## Details

本任务在 `feat/nhtsa-recalls-connector` 分支完成，提交 `0da1a9f`。

实现要点：

- 新增 `worker/sentineldrive_worker/connectors/nhtsa_recalls.py`：`NhtsaRecallsConnector` 复用 `HttpClient`、`RawIntelligencePayload`、`ConnectorError`。按配置车辆清单逐 `make/model/modelYear` 查询 NHTSA `recallsByVehicle` 端点；`recallsByMake`/`campaigns` 端点不做（按任务说明需认证，403）。
- 采集层降噪：`overTheAirUpdate` 布尔位或 Component/Summary 命中 software/telematics/cyber/OTA/firmware 等关键词才打 `software_related`；其余机械召回直接过滤不产出。
- 去重与幂等：campaign number（如 `22V063000`）作为 external ID，cursor 存 `seen_campaigns` 集合，跨轮不重复产出。
- 留存：API JSON 走 `raw_payload` 模式；`raw_content` 只保留公开安全召回字段（campaign/make/model/modelYear/component/summary 等），不回传任何 VIN/PII。
- 配置：`config.py` 新增 `DEFAULT_NHTSA_VEHICLES`（Tesla 全系 5 台 + Rivian R1T/R1S + Chevy Bolt EV/EUV，共 9 台）与 `DEFAULT_NHTSA_MODEL_YEARS=4`，接入 `_default_env_sources` 的 `nhtsa-recalls` 源（`SOURCE_ENABLED_NHTSA_RECALLS` 默认 false）；`DEFAULT_VENDOR_ENDPOINTS` 追加 Vector Informatik / Wind River / Geely GSRC / Xiaomi SRC 四个端点。
- 源特定 normalizer：`RecallNormalizer`（`source_name=nhtsa-recalls`）把召回归 `intelligence_type=incident`，tags 打 `recall`+`software_related`+`nhtsa`，`affected_vendor=make`、`affected_product=model`，dedup 键 `recall:campaign:<campaign>`，保留 `SourceAttribution`。
- 注册：`samples.py` 注册 `nhtsa-recalls` connector，`normalizers.py` 注册 `nhtsa-recalls` normalizer。
- 文档同步（按 AGENTS.md 要求）：`docs/connectors.md`、`docs/source-configuration.md`、`docs/environment.md` 记录新 Connector、`SOURCE_ENABLED_NHTSA_RECALLS` 变量与新增厂商端点。

未改后端、前端、docker-compose，未引入通用威胁情报库、工单/AI 分析/新通知渠道。

## Output

修改/新增文件：

- `worker/sentineldrive_worker/connectors/nhtsa_recalls.py`（新增）
- `worker/sentineldrive_worker/connectors/config.py`
- `worker/sentineldrive_worker/connectors/samples.py`
- `worker/sentineldrive_worker/normalization/normalizers.py`
- `worker/tests/test_nhtsa_recalls_connector.py`（新增）
- `worker/tests/test_normalization_pipeline.py`
- `docs/connectors.md`、`docs/source-configuration.md`、`docs/environment.md`

提交：

- `0da1a9f feat: 新增 NHTSA 召回 connector 并扩展厂商公告端点`

## Validation

- `.venv/bin/python -m pytest worker/tests -q`：105 passed（含既有 88 条 + 新增 16 条 connector + 1 条 normalizer）。
- 新增 mock 测试覆盖：happy path（campaign→external_id、software_related 标记、安全 raw 字段）、非 2xx/畸形 JSON/字段缺失（抛 `ConnectorError` 或宽容跳过）、`Count=0` 空结果、车辆间失败隔离、软件/机械过滤、seen_campaigns 跨轮幂等、vendor 端点扩充后可解析、`nhtsa-recalls` 源默认关闭/按环境变量启用。全部 mock，不触真实网络。

## Issues

无未解决阻塞。

## Important Findings

- **并发分支污染（重要）**：执行期间后端访客只读（Task 10.1）在**同一工作目录**并发操作，中途把共享 HEAD 从 `feat/nhtsa-recalls-connector` 切回 `feat/guest-readonly-backend`，导致本任务提交 `0da1a9f` 一度落在 `feat/guest-readonly-backend` 上。我已用仅 ref 操作（`git branch -f` + 同 commit checkout，未触碰工作区、未 `reset --hard`）修正：`feat/nhtsa-recalls-connector`=0da1a9f，`feat/guest-readonly-backend`=60289df（后端 Task 10.1 的提交）。注意 `0da1a9f` 的父提交是 `60289df` 而非 `main`（因并发切分支所致）；Manager 合并时若需「纯 main + 本任务」的分支，可对 `feat/nhtsa-recalls-connector` 执行 `git rebase --onto main 60289df`。并行派发应使用独立 worktree，当前共享单一工作目录存在互相切换分支的风险。
- **NHTSA 端点未做真实联网验证**：本任务按要求全部 mock，未实测 `recallsByVehicle` 响应字段名。Connector 用了 `NHTSACampaignNumber`、`ReportReceivedDate`、`Component`、`Summary`、`OverTheAirUpdate` 等 camelCase 字段名并带 snake_case 回退，解析对缺失字段宽容；若真实响应的字段名或 `OverTheAirUpdate` 类型（布尔/字符串）与假设不符，需要按实际 JSON 微调，不影响正常化与去重链路。