# SentinelDrive 车联网威胁情报平台需求说明

## 1. 项目定位

SentinelDrive 是一个面向车联网安全研究和安全运营的威胁情报平台。平台需要 24 小时运行，持续收集、整理、检索、评分和告警与车联网相关的安全情报。

车联网安全的攻击面覆盖移动 App、云平台、T-Box、车机 IVI、OTA、CAN、V2X、充电设施、通信模组、芯片、操作系统和供应链组件。通用漏洞平台通常只能描述 CVE、CVSS、CWE 等通用字段，无法很好表达「是否影响车辆远控」「是否影响 OTA」「是否涉及车内网络」「是否影响充电或 V2X」等车联网业务语义。

本项目希望在通用漏洞情报的基础上，增加车联网场景下的组件分类、攻击面分类、风险评分和研判字段。

## 2. 项目目标

核心目标：

- 持续收集公开威胁情报和人工录入情报。
- 将不同来源的原始情报规范化为统一数据模型。
- 对重复情报进行去重、合并和关联。
- 支持按 CVE、供应商、组件、攻击面、风险等级、标签等维度检索。
- 对高风险情报进行基础评分和告警。
- 支持后续方便地增加更多数据源。
- 先使用 Docker Compose 在单台 Ubuntu 服务器运行，后续可迁移到云数据库、消息队列、对象存储、多节点 Worker 或 Kubernetes。

## 3. 项目名称

项目名称暂定为 **SentinelDrive**。

含义：

- **Sentinel：** 哨兵、监控、预警。
- **Drive：** 车辆、驾驶、车联网场景。

整体定位是「车联网威胁情报哨兵」。

## 4. 目标用户

| 用户角色 | 主要诉求 |
| --- | --- |
| 安全研究人员 | 跟踪车联网漏洞、分析攻击链、沉淀研究线索 |
| 情报分析人员 | 汇总多来源情报、去重、标注、研判风险 |
| 安全运营人员 | 关注高风险漏洞、配置告警、跟踪处置状态 |
| 车联网学习者 | 学习车联网攻击面、漏洞类型和情报分析流程 |
| 管理人员 | 查看风险趋势、重点漏洞和处置进展 |

## 5. MVP 范围

MVP 阶段优先完成主链路，先做可运行原型，不做复杂自动化研判。

### 5.1 必须支持的数据源

| 数据源 | 类型 | 说明 |
| --- | --- | --- |
| NVD CVE | 漏洞数据库 | 收集 CVE 基础信息、CVSS、CWE、影响产品等 |
| CISA KEV | 已知被利用漏洞 | 收集已被真实利用的高优先级漏洞 |
| 厂商安全公告 | 供应商公告 | 收集车企、芯片、T-Box、车机、通信模组、操作系统等供应商公告 |
| RSS | 安全资讯 | 收集安全博客、研究团队、厂商安全中心的 RSS |
| 手动录入 | 人工情报 | 支持用户手动录入漏洞、事件、资产暴露或研究线索 |

### 5.2 必须完成功能

- 情报采集：定时拉取数据源，记录原始数据和来源。
- 情报规范化：将不同来源映射为统一 Threat Intelligence 数据模型。
- 去重与关联：基于 CVE ID、URL、标题、内容 hash 和来源字段做基础去重。
- 检索与过滤：支持按关键字段查询和筛选。
- 风险评分：根据漏洞严重性、是否已知利用、是否远程可利用、是否影响车辆关键组件等因素计算基础风险等级。
- 基础告警：对高风险情报生成告警记录。
- 前端工作台：提供情报列表、详情页、筛选、搜索、数据源状态和告警查看。
- 运行日志：记录采集任务、失败原因、重试次数和最近同步时间。

### 5.3 暂不包含能力

- 暗网采集。
- 知识图谱。
- AI 自动研判。
- 自动生成攻击链。
- 自动处置和工单流转。
- 大规模恶意样本分析。
- 多租户商业化权限体系。
- 真实车辆远控、车端 IDS 或 CAN 总线实时检测。

## 6. 可扩展性要求

平台必须支持后续方便地增加更多数据源。采集模块应采用 Source Adapter / Connector 模式。

### 6.1 Connector 设计原则

每个数据源实现为独立 Connector。新增数据源时，不应修改核心入库、搜索、评分和告警逻辑。

每个 Connector 至少负责：

- 数据源配置。
- 数据拉取。
- 限速。
- 重试。
- 错误处理。
- 原始数据保存。
- 来源标记。
- 增量同步状态记录。
- 输出统一 Raw Intelligence 结构。

### 6.2 后续可扩展数据源

| 数据源 | 说明 |
| --- | --- |
| GitHub Security Advisory | 开源组件漏洞公告 |
| GitHub 代码搜索 | 发现泄露 Token、密钥、固件链接和 PoC 线索 |
| 车企安全公告 | 收集车企官方安全通报 |
| 供应商公告 | 收集芯片、模组、车机、T-Box、OS、OTA 供应商公告 |
| 安全会议论文 | 收集 Black Hat、DEF CON、Pwn2Own Automotive 等研究内容 |
| Exploit 数据库 | 跟踪公开 PoC 和 Exploit |
| 社交媒体 | 跟踪研究人员和安全团队发布的线索 |
| 论坛和博客 | 收集漏洞分析文章、事件复盘和技术报告 |
| 内部事件 | 接入内部渗透测试、红队、漏洞扫描和日志告警结果 |

### 6.3 扩展性验收标准

- 新增一个 RSS 数据源时，只需要增加配置或新增一个 Connector。
- 新增一个 API 型数据源时，不需要修改数据库核心模型。
- Connector 输出结构稳定，后续规范化流水线可复用。
- 采集 Worker 可以横向扩展，后续允许多个节点并行运行。
- 数据源启停、同步频率、超时时间、限速参数应可配置。

## 7. 核心业务流程

```text
Source Connector -> Raw Intelligence -> Normalize -> Deduplicate -> Threat Intelligence -> Search / Score / Alert
```

处理步骤：

1. Scheduler 定时触发采集任务。
2. Worker 调用对应 Connector 拉取数据。
3. Connector 保存原始响应、来源 URL、同步时间和 hash。
4. 规范化模块将原始数据映射为统一字段。
5. 去重模块根据 CVE ID、URL、标题、内容 hash 等生成 dedup key。
6. 风险评分模块计算风险等级。
7. 告警模块对高风险情报生成告警。
8. 前端和 API 提供查询、筛选、详情和告警查看。

## 8. 数据模型需求

### 8.1 Raw Intelligence

Raw Intelligence 用于保存原始采集结果，便于复查和重新解析。

| 字段 | 说明 |
| --- | --- |
| id | 原始情报 ID |
| source_name | 来源名称，例如 NVD、CISA KEV |
| source_type | 来源类型，例如 API、RSS、HTML、Manual |
| source_url | 原始 URL |
| fetched_at | 采集时间 |
| raw_content | 原始内容 |
| raw_hash | 原始内容 hash |
| status | 采集状态 |
| error_message | 失败原因 |

### 8.2 Threat Intelligence

Threat Intelligence 是规范化后的核心情报对象。

| 字段 | 说明 |
| --- | --- |
| id | 情报 ID |
| title | 标题 |
| summary | 摘要 |
| intelligence_type | 情报类型，例如 Vulnerability、Exposure、Incident、Advisory |
| source_names | 来源列表 |
| source_urls | 来源 URL 列表 |
| cve_id | CVE ID |
| cwe_id | CWE ID |
| cvss_score | CVSS 分数 |
| severity | 严重等级 |
| affected_vendor | 受影响厂商 |
| affected_product | 受影响产品 |
| affected_version | 受影响版本 |
| vehicle_component | 车联网组件，例如 App、T-Box、IVI、OTA、V2X、Charging |
| attack_surface | 攻击面，例如 Cloud API、Bluetooth、Wi-Fi、Cellular、CAN、USB |
| exploit_status | 利用状态，例如 Unknown、PoC Available、Known Exploited |
| confidence | 可信度 |
| risk_score | 平台风险评分 |
| tags | 标签 |
| first_seen_at | 首次发现时间 |
| last_seen_at | 最近更新时间 |
| dedup_key | 去重键 |
| status | 处理状态，例如 New、Triaged、Ignored、Resolved |

## 9. 风险评分需求

MVP 阶段使用规则评分，不引入复杂 AI 研判。

评分因素包括：

- CVSS 分数。
- 是否进入 CISA KEV。
- 是否存在公开 PoC。
- 是否远程可利用。
- 是否无需认证。
- 是否影响车联网关键组件。
- 是否涉及远程控制、定位、OTA、车门、启动、充电或车内网络。
- 是否影响多个供应商或通用组件。
- 情报来源可信度。

风险等级建议：

| 等级 | 分数范围 | 说明 |
| --- | --- | --- |
| Critical | 90-100 | 高危且可能影响车辆安全或已被利用 |
| High | 70-89 | 影响范围较大或存在明确利用路径 |
| Medium | 40-69 | 有安全影响，但利用条件较高 |
| Low | 0-39 | 影响有限或信息不足 |

## 10. 告警需求

MVP 阶段先实现基础告警。

触发条件包括：

- 新增 Critical 情报。
- 新增 CISA KEV 漏洞。
- 新增影响 T-Box、OTA、Cloud API、App 远控、CAN、V2X、Charging 的高风险情报。
- 同一 CVE 在多个来源重复出现。
- 某供应商或组件在短时间内出现多条高风险情报。

告警记录至少包含：

- 告警标题。
- 关联情报 ID。
- 触发规则。
- 风险等级。
- 触发时间。
- 当前状态。
- 备注。

## 11. 前端工作台需求

MVP 前端应提供以下页面：

- 情报列表页：支持搜索、筛选、排序。
- 情报详情页：展示来源、摘要、影响对象、风险评分、原文链接、关联告警。
- 告警列表页：展示高风险告警和处理状态。
- 数据源管理页：查看数据源启停、最近同步时间、失败次数。
- 手动录入页：录入新的情报或研究线索。

前端设计应偏安全运营工具风格，信息密度适中，避免营销型页面。

## 12. 技术架构建议

### 12.1 MVP 技术栈

| 模块 | 建议 |
| --- | --- |
| 后端 API | FastAPI |
| 采集 Worker | Python + Celery / RQ / APScheduler |
| 定时调度 | Celery Beat / APScheduler |
| 数据库 | PostgreSQL |
| 缓存和队列 | Redis |
| 搜索 | PostgreSQL 全文搜索起步，后续可接 Meilisearch 或 OpenSearch |
| 前端 | Next.js / React |
| 部署 | Docker Compose |
| 反向代理 | Nginx / Caddy |
| 监控 | 日志 + Prometheus / Grafana 后续接入 |

### 12.2 部署要求

MVP 先部署在单台 Ubuntu 服务器，使用 Docker Compose 管理服务。

服务建议包括：

- frontend
- backend
- scheduler
- worker
- postgres
- redis
- reverse-proxy

后续可拆分为：

- 云数据库。
- 云 Redis。
- 对象存储。
- 多节点 Worker。
- Kubernetes。
- 海外采集节点。

## 13. 服务器与网络要求

当前服务器可作为 MVP 主部署节点。

注意事项：

- 服务器在国内地域时，访问 NVD、CISA、GitHub、国外安全博客等外网来源可能不稳定。
- 后续建议增加腾讯云香港或新加坡节点作为外网采集 Worker。
- 数据库端口不应暴露到公网。
- 公网只开放 SSH、HTTP 和 HTTPS。
- 采集任务需要限速，避免对目标站点造成压力。

## 14. 安全与合规要求

平台自身也需要基本安全设计。

要求包括：

- 管理后台必须登录后访问。
- 密码和 API Token 不允许硬编码。
- 数据库、Redis、搜索服务不暴露公网。
- 采集器需要遵守目标站点的服务条款和合理访问频率。
- 不在前端直接展示未脱敏的隐私数据、账号凭据、Token、车主 VIN 批量数据等敏感内容。
- 操作日志需要记录关键动作，例如手动录入、状态变更、告警关闭。
- 原始情报应保留来源 URL 和采集时间，便于审计。

## 15. 非功能需求

| 类型 | 要求 |
| --- | --- |
| 可用性 | 采集服务应支持 24 小时运行，单个数据源失败不影响其它数据源 |
| 可维护性 | Connector 独立实现，核心逻辑清晰分层 |
| 可观测性 | 记录任务日志、失败原因、同步时间和处理数量 |
| 可扩展性 | 支持新增数据源和横向扩展 Worker |
| 可恢复性 | 任务失败可重试，系统重启后可继续增量同步 |
| 数据可靠性 | 原始数据和规范化数据都应保存，便于重新解析 |
| 部署简洁性 | MVP 使用 Docker Compose 一键启动 |

## 16. 里程碑

### Milestone 1：项目骨架和基础部署

- 初始化项目结构。
- 配置 Docker Compose。
- 启动 FastAPI、PostgreSQL、Redis。
- 提供健康检查接口。

### Milestone 2：情报采集和原始数据入库

- 实现 NVD Connector。
- 实现 CISA KEV Connector。
- 实现 RSS Connector。
- 保存 Raw Intelligence。
- 记录采集日志和同步状态。

### Milestone 3：规范化、去重和核心模型

- 定义 Threat Intelligence 数据模型。
- 实现规范化流水线。
- 实现 dedup key。
- 支持重复情报合并来源。

### Milestone 4：搜索、筛选和详情 API

- 实现情报列表 API。
- 实现详情 API。
- 支持按 CVE、来源、组件、风险等级、标签过滤。

### Milestone 5：风险评分和基础告警

- 实现规则评分。
- 实现 Critical / High 告警。
- 提供告警列表和状态更新。

### Milestone 6：前端工作台

- 实现情报列表页。
- 实现情报详情页。
- 实现告警列表页。
- 实现数据源状态页。
- 实现手动录入页。

### Milestone 7：监控、文档和上线

- 补充部署文档。
- 补充运维命令。
- 增加备份方案。
- 增加基本监控和日志查看方式。

## 17. 验收标准

MVP 完成时，应满足以下条件：

- 系统可以通过 Docker Compose 在 Ubuntu 服务器启动。
- 至少支持 NVD、CISA KEV、RSS 和手动录入。
- 采集任务可以定时运行，并记录成功和失败状态。
- 原始情报和规范化情报都能入库。
- 相同 CVE 不会重复生成多条核心情报。
- 用户可以在前端搜索和筛选情报。
- 用户可以查看情报详情和来源链接。
- 高风险情报可以生成告警。
- 新增一个 RSS 源不需要修改核心业务代码。
- 单个 Connector 失败不会导致整个平台不可用。

## 18. APM Planner 启动语句

可以在 Codex 中使用以下语句启动 APM Planner：

```text
$apm-1-initiate-planner 我要开发一个车联网威胁情报平台 SentinelDrive，目标是 24 小时运行收集情报。MVP 先支持 NVD CVE、CISA KEV、厂商安全公告、RSS 和手动录入，完成采集、标准化、去重、搜索、风险评分和基础告警。系统需要具备良好扩展性，采集模块必须采用 Source Adapter / Connector 模式，后续可以方便增加 GitHub Security Advisory、厂商安全公告、社交媒体、安全博客、漏洞报告、供应链公告和内部事件等其它信息源。部署目标是先用 Docker Compose 在单台 Ubuntu 服务器运行，架构设计要避免绑定单机实现，后续可以平滑迁移到云数据库、消息队列、对象存储、多节点 Worker 或 Kubernetes。请先收集上下文、识别缺口并向我提问，不要直接写代码。
```

## 19. 待确认问题

这些问题需要在 APM Planner 阶段继续确认：

- 平台是否只给个人使用，还是会有多人协作和权限分级？
暂时个人使用
- 是否需要支持中文和英文双语界面？
中文
- 是否需要对接企业微信、飞书、邮件或 Telegram 告警？
暂时不需要
- 厂商安全公告第一批要支持哪些厂商？
  车企：上汽、长安、吉利、比亚迪、蔚来、小鹏、理想、长城、广汽等。
  芯片：高通、联发科、华为海思、展讯、瑞芯微等。
  车机：安卓车机、Wind River VxWorks、QNX、Aptiv、BlackBerry QNX 等。
  T-Box：德赛西威、华为 HiCar、四维图新等。
  OTA：博世、大陆、华为、阿里巴巴等。
- 是否需要保存原始情报
保留链接
- 是否需要保存原文网页快照和 PDF 附件？
保留链接
- 是否需要接入 GitHub API？如果需要，是否准备 Token？
暂时不需要
- 是否需要部署海外采集节点？
暂时不需要
- 是否需要用户自定义风险评分规则？
暂时不需要，先做固定规则
- 是否需要导出 CSV、Markdown 或 PDF 报告？
需要
- 是否需要把需求和 APM 文档同步到 Git 仓库？
不需要
