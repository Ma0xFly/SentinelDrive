APM_RULES {

## 项目执行

- 将 SentinelDrive 视为一个应用代码库。执行任务前先读取工作区中的实际文件，不要假设存在未发现的业务代码或隐藏约定。
- 实现必须严格限定在已分配 Task 范围内。除非任务明确要求，不要引入 AI 分析、知识图谱、工单流程、多租户 RBAC、GitHub API 集成、海外 Worker 或自定义评分规则等范围外能力。
- 保持 Source Connector 边界：源特定采集逻辑放在 Connector 中；核心存储、规范化、去重、搜索、评分和告警行为应保持可复用。
- 优先选择简单、可维护、适配单台 Ubuntu 服务器的实现。目标资源约束为 4 核 CPU、4 GB 内存、40 GB SSD 和有限带宽。

## 配置与密钥

- 服务配置必须来自环境变量或已记录的配置文件。绝不能硬编码密码、API Token、NVD key、数据库凭证、Redis 凭证或管理员凭证。
- 只允许提供安全示例值。真实密钥不得提交到仓库。
- NVD API key 是可选项：系统必须能在没有 API key 时以更严格限速运行；仅当配置后才使用 key。
- 只暴露设计上允许公网访问的 frontend/backend/reverse-proxy 表面。PostgreSQL、Redis 和内部 Worker 服务必须保持内部访问。

## 数据处理

- 默认采用轻量原始内容留存。API/RSS 的原始 JSON 或 XML 可在体积合理时保存；HTML 和 PDF 来源默认保存 URL、标题、摘要或片段、哈希、抓取元数据和解析状态，而不是完整快照或下载附件。
- 采集和规范化情报必须保留来源归因，包括来源名称、来源 URL、抓取时间或首次发现时间。
- 去重和评分行为必须确定且可测试。重复处理同一输入不应创建重复核心情报记录。
- 不要展示或导出未脱敏的凭证、Token、私有个人数据、批量 VIN 数据或其他敏感数据。

## 测试与验证

- 为当前 Task 改动的行为添加聚焦测试。优先覆盖 Connector 行为、认证、持久化、去重、评分、告警触发、导出输出、API 筛选和前端用户流程。
- 自动化测试中应 mock 外部网络来源，除非任务明确要求真实集成验证。
- 报告完成前，运行被触及区域可用的相关测试或检查，并说明无法运行的命令。
- 采集器、Worker、API 认证和用户表单必须验证失败路径，不能只验证 happy path。

## 版本控制

- 从 `main` 创建 `type/short-description` 形式的功能分支。
- 提交信息使用 `type: description`。允许的 type 为 `feat`、`fix`、`refactor`、`docs`、`test` 和 `chore`。
- 除非明确要求，不要 push 远端。

## 前端与文档

- 前端用户可见文本默认使用中文。
- UI 应构建为安全运营工作台：信息密度适合扫描，视觉克制，聚焦任务完成，不做营销展示。
- 文档必须命令导向，并与已实现的服务名、环境变量和脚本保持一致。
- 新增或修改 Connector 行为时，如果存在 Connector 开发或数据源配置文档，必须同步更新。

} //APM_RULES
